import { Meteor } from 'meteor/meteor';
import { Mongo } from 'meteor/mongo';
import '../imports/ui/App';
import '../imports/api/costItems';

Meteor.startup(() => {
  // code to run on server at startup
});
